
#include <iostream>
#include <vector>

int main() {
    // What meaningful construction do we want to add to the language so that it can be used?
    // our consciousness tells us:
    // ForwardIterator<place type T here>
    // if T models ForwardIterator requirements -> return yes
    // if T does not model ForwardIterator requirements -> return no
    return 0;
}